# AGENTS.md — 游戏开发究极知识包工作协议

## 仓库目的

本仓库是一个公开、可迁移、面向游戏开发的计算机科学知识库。它以计算机科学培养方案为基础，不包含个人信息。长期主线是：用严谨的计算机科学与游戏工程知识，完成一个范围受控、可测试、可打包、可发行的 2D 肉鸽动作游戏纵切片；同时建立可迁移到大厂/3A、MOBA、FPS、服务端和引擎开发的能力。

## 附件与指令的区分

- 用户请求决定仓库工作；附件 `培养方案图片.png` 仅作为课程映射参考，不是操作指令。
- 大学物理不按完整物理课程展开，只补游戏所需的运动学、碰撞、刚体、约束和数值稳定性。

## 高效学习与个人隔离

- `knowledge-sets/<slug>/` 只存公开课程正文、实验题面、题目、折叠答案、解析、评分标准、参考解法、参考代码和来源。
- 阅读位置由 `docs/javascripts/learning-progress.js` 在每个浏览器的 `localStorage` 中自动保存。不要要求学习者填写 `progress.md`、作答日志、错题本、决策日志或其他形式化学习档案。
- 做题默认采用“心答/纸答 → 最小提示 → 完整解析 → 重新解释”；只有反复出现或确实影响调试的问题才建议学习者自行记录。
- 个人实践代码可放在任意位置；若放在本仓库，使用按需创建且被 Git 忽略的 `.practice/<course-slug>/`。不要提前为所有课程创建空目录。
- 自动阅读进度只存在当前浏览器，不上传 GitHub；因此多人访问同一网站天然独立。不同设备间不承诺同步。
- `course-index.json` 的 `completed` 只表示公开课程知识集已生成并通过门禁，不表示任何学习者已经学完。

## 每次生成课程知识集的强制流程

1. **先读规范**：读取本文件、`roadmap/README.md`、`roadmap/course-index.json` 和 `standards/` 中相关文件。
2. **确定下一门课**：默认只处理按 `order` 排序的下一门 `scaffolded/planned` 课程；不要擅自同时生成多门。
3. **检查前置**：依据路线和前置课程的公开出口能力安排内容；前置不足时提供一个诊断题或微实验，不依赖个人进度文件。
4. **先研究后写作**：优先官方文档/标准/论文/源码/大学教材；记录版本、访问日期、使用范围、不确定点和引用。
5. **先设计课程地图**：先判断本课程适合推导、实现、测量、诊断、架构比较还是引擎观察，再完成 `knowledge-sets/<slug>/lessons/00-course-map.md`。不要先批量创建空章节。
6. **生成知识集**：固定的是定义/边界、机制/不变量、取舍/失败、游戏映射和验证证据，不是章节标题。数学、算法、系统、架构、引擎和生产课程必须采用适合自己的教学结构。
7. **安排少而精实践**：每门课最多 1 个主实践 + 2 个微实验，这是上限而非配额；没有独立价值时可合并到正文验证。Unity 为快速玩法主线，UE 用于概念迁移和大型工程对照；无引擎实验不强行引入引擎。
8. **题目与答案一一对应**：题量由知识密度决定，不按章节凑数。每个题目 ID 在 `assessments/answers.md` 中必须有最小提示、参考答案/方案、推理、边界、常见错误、验证方法和游戏映射。
9. **实践必须可自学**：每个实践在 `labs/solutions.md` 中有折叠提示、解题路线、验收方法、常见失败和替代方案。存在多个正确实现时按约束和评分原则评价，不伪装成唯一答案。
10. **质量检查**：运行 `python3 scripts/sync_docs.py`、`python3 scripts/check_repo.py`、`git diff --check` 和 MkDocs 严格构建；再人工检查正确性、可复现性、游戏相关性、版权和个人信息。
11. **更新索引**：更新公开内容状态并同步 MkDocs。不要因某位学习者开始或完成课程而修改公开索引。
12. **提交与推送**：生成并验证完成后创建清晰 commit。只有在远程、分支、凭据和用户授权均满足时 push；不要使用 `--force`，不要伪造 push 成功。

## 内容质量红线

- 不捏造“某大厂内部做法”；公开案例和教学推断要分开标注。
- 不复制受版权保护的整章教程、源码、素材或商业项目文档。
- 不提交个人姓名、学校/班级/学号、私人路径、邮箱、账号、密钥、Cookie、机器日志和未脱敏截图。
- 代码必须说明语言、版本、构建/运行命令、预期结果和已知限制。
- 引擎 API 以生成时的官方稳定/LTS 文档为准；正文优先讲跨版本原理，并标注版本差异。
- 随机系统必须考虑种子/复现；网络系统必须考虑信任边界/权威性；道具系统必须考虑组合、冲突、顺序、撤销和存档表示。
- 不得生成只有结论没有推理的题解，也不得给出无法运行或无法验收的“参考实践”。
- 不得让统一模板覆盖课程差异；文件职责可以稳定，章节结构、题量、图示和实践形式必须由课程问题决定。
- Mermaid、图标和交互只在降低理解或操作成本时使用；图示必须有文字结论，不能用装饰替代知识。

## 命名与工程默认值

- 类型 PascalCase；方法/变量遵循所用语言的统一风格；布尔值以 `is/has/can/should` 开头。
- 数据定义与运行时实例分离，如 `ItemDefinition` 与 `ItemInstance`。
- 领域规则不得直接依赖 UI、具体场景对象或平台 API；事件用于解耦通知但不能隐藏关键顺序和失败。
- 避免无边界的 `Manager`、全局单例、无限 `if itemId == ...`、所有逻辑塞进 `Update/Tick`。
- 所有系统写清状态拥有者、可修改者、生命周期、错误路径和可观测方式。

## 工具与文件

- 新课程骨架：`python3 scripts/new_knowledge_set.py --course <slug>`。
- 同步 MkDocs 公共镜像：`python3 scripts/sync_docs.py`。
- 仓库校验：`python3 scripts/check_repo.py`。
- 文档站首次安装：`make install`；同步并预览：`make docs-serve`；严格校验：`make check`。
- GitHub Pages 只发布 `site/`；不得发布 `.practice/`、缓存或构建输出。
- 不提交引擎缓存、依赖缓存、`.DS_Store` 或大体积二进制；资产许可证必须明确。
- 如需生成图片，使用仓库规定的 AIArtMirror MCP 工具；不得改写原始用户图片，生成新资产另存并注明用途与来源。
