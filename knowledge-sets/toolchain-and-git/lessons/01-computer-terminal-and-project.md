# 1. 从“文件夹里的代码”开始：程序、终端与项目

如果你是第一次系统学习编程工具，最容易遇到的不是复杂算法，而是这些问题：

- “我双击了文件，为什么没有运行？”
- “教程让我在终端输入命令，终端到底在控制什么？”
- “我改了代码，为什么游戏还是旧的？”
- “这个目录能删吗？删了还能恢复吗？”

本章先不讲分支、CI 或构建边界。我们只建立后面所有章节都会使用的五个词：**文件、目录、路径、进程、退出码**。

## 机制

终端命令不是“对项目施魔法”，而是由 shell 解析输入、启动某个程序，并让程序读取当前目录、环境和参数后产生文件变化或退出状态。排查“没生效”时，必须沿着这条状态链确认实际输入和产物，而不是只看屏幕上的最后一句输出。


## 1.1 文件、目录和路径

**文件（file）**是一段有名字的数据，例如 `player.c`、`Game.cs`、`room.json` 或 `hero.png`。扩展名通常提示工具如何解释内容，但改名不会自动改变内容本身。

**目录（directory/folder）**用于组织文件。目录可以包含子目录，所以项目形成一棵树：

```text
mini-game/
├── README.md
├── src/
│   └── game.py
├── tests/
│   └── test_game.py
└── assets/
    └── hero.png
```

**路径（path）**是从某个起点找到文件的路线：

- 绝对路径从磁盘根位置开始，只适合当前机器；
- 相对路径从“当前工作目录”开始，更适合项目命令和协作。

例如当前目录是 `mini-game/` 时，`src/game.py` 是相对路径。课程代码、日志和构建脚本应优先记录仓库相对路径，避免把个人用户名和机器目录写进公开结果。

## 1.2 终端不是黑色魔法，而是给程序输入命令

终端（terminal）提供一个文字界面；shell（例如 zsh、bash 或 PowerShell）读取命令，找到对应程序并启动它。

以下命令从知识库根目录开始，先建立个人副本；若已有实践，初始化脚本应保留它，遇到拒绝不要覆盖。验证 Git 忽略后再进入副本：

```bash
python3 scripts/init_practice.py --course toolchain-and-git
git check-ignore -v .practice/toolchain-and-git
git status --short --untracked-files=all
cd .practice/toolchain-and-git/repro-game
pwd                         # 打印当前工作目录
ls                          # 列出当前目录内容
python3 --version           # 启动 Python，要求它打印版本
```

每条命令都可以按四个问题理解：

| 问题 | `cd .practice/toolchain-and-git/repro-game` 的答案 |
|---|---|
| 读取什么？ | 当前目录和目标相对路径 |
| 改变什么？ | 当前 shell 之后解释相对路径的起点 |
| 不改变什么？ | 不修改项目文件，也不进入 Git 历史 |
| 如何判断？ | 再运行 `pwd` 和 `ls` |

初学时不要一次复制十条命令。先运行一条，观察输出，再运行下一条。看到错误时先读第一行和最后一行，确认是“命令找不到”“路径不存在”还是程序本身失败。

## 1.3 程序、进程、标准输出和退出码

磁盘上的 `game.py` 是文件；运行它时，操作系统创建一个**进程（process）**。进程读取输入、占用内存、产生输出，最后结束。

```bash
python3 src/game.py --seed 42
```

这条命令中：

- `python3` 是要启动的程序；
- `src/game.py` 是 Python 要读取的脚本；
- `--seed 42` 是传给脚本的参数；
- 屏幕文字通常来自标准输出或标准错误；
- 进程结束时返回**退出码（exit code）**，通常 `0` 表示成功，非 `0` 表示失败。

在 zsh/bash 中可以观察上一条命令的退出码：

```bash
python3 src/game.py --seed 42
echo $?
```

自动测试和 CI 不能像人一样“看起来差不多”。它们主要依赖退出码和结构化报告判断成功或失败，所以一个失败后仍返回 `0` 的脚本会制造假绿灯。

## 1.4 源文件、生成文件和“我改了为什么没生效”

一个游戏项目往往同时存在：

- 人编辑的源文件；
- 工具根据源文件生成的中间文件；
- 可以运行或发布的最终产物；
- 为了加速保存的缓存。

你修改了 `src/game.py`，却启动了旧的 `dist/game.py`，看到的当然还是旧行为。先用路径确认自己运行的是哪一份：

```bash
python3 src/game.py --seed 42
python3 dist/game.py --seed 42   # 只有构建过后才存在
```

后面第 6 章会正式建立构建系统模型。现在只记住：**编辑对象、运行对象和发布对象可能不是同一个文件。**

## 1.5 第一次项目观察

在刚才的 `.practice/toolchain-and-git/repro-game/` 个人副本中运行（不要在公开参考目录运行可能产生缓存的测试）：

```bash
pwd
find . -maxdepth 2 -type f | sort
python3 -m unittest discover -s tests -v
python3 src/game.py --seed 42
```

逐项回答：

1. 源码在哪里？
2. 测试在哪里？
3. 哪条命令改变当前 shell 目录，哪条启动外部程序？启动进程与修改文件是否是同一回事？
4. 测试成功时退出码是什么？
5. 改变 seed 后，哪些输出改变，哪些不应改变？

## 本章连接

现在你已经知道“项目目录中有状态，命令会读取或改变状态”。第 2 章引入 Git：它不会自动理解程序是否正确，而是帮助我们精确记录**哪些文件状态准备进入下一次项目快照**。


## 1.6 复制命令前必须知道的 shell 边界

本课程命令块采用 bash/zsh 的语法；PowerShell 的变量、退出码与重定向规则不同，不能混着复制。`cd` 是当前 shell 的内建命令；python3、Git 等一般作为子进程运行。即使 ls 只读取目录，它通常也会启动进程，所以“只读”和“启动进程”不是二选一。

`>` 创建或覆盖文件，`>>` 追加；`2>` 单独处理标准错误；`|` 把前一程序标准输出送入后一程序。`$?` 只保存刚结束命令的退出码，所以先执行 ls 再 echo 得到的不是更早的测试结果。`&&` 让后一步只在前一步成功后执行；`;` 则不管前一步是否成功都继续。

变量取路径时加双引号，例如 `cd "$git_lab/work"`，避免路径中的空格被拆成多个参数。`$(command)` 获取命令的标准输出；`mktemp -d` 创建新的临时目录，不复用别人可能正在使用的固定路径。后续章节用它造一次性 Git 情景，不要求把情景当成第二个主实践或学习日志。`<commit>` 这类尖括号占位必须替换为实际提交 ID，不能原样输入。

后面的验证会用 `test`：`test -f path` 判断文件存在且是普通文件；`test ! -e path` 判断路径不存在；`test -z "$text"` 判断字符串为空；`test "$a" = "$b"` 比较字符串。成立退出 0，不成立退出非零，失败本身不一定打印提示。`if command; then ...; fi` 按 command 的退出码选分支，`exit 1` 会结束当前 shell/脚本，所以发现“预期失败反而成功”的保护分支应停止，而不是继续粘贴。

连续情景中的命令默认逐条执行并检查错误；维护者回归脚本使用 `set -eu` 在未处理失败或未设置变量时停止。预期失败放进 if 显式判断，而不是依赖忽略所有错误。`<<'PYTEST'` 到独立一行 PYTEST 是 here-document，把其间的 Python 文本交给 `python3 -` 从标准输入运行；分隔符加引号使 shell 不展开其中的 `$`。这些 Python 代码是可直接运行的验证器，不要求先掌握 Python 才能理解 Git 引用变化。

## 本章练习

### T01-Q1：终端命令的状态

pwd、git status 和 `python3 tool.py` 分别读取/改变什么？

<details><summary>最小提示</summary>

把命令分成观察、修改工作区、启动进程三类。
</details>

<details><summary>讲解与验证</summary>

`pwd` 和 `git status` 主要读取当前环境/仓库状态；运行 Python 启动进程并可能产生文件。用 `git status --short --untracked-files=all` 和 `echo $?` 验证。常见错误是把任何命令都当成会提交或上传。游戏映射：构建命令可能写入产物，但不应自动修改源文件。
</details>

### T01-Q2：为什么“重新运行”可能仍然是旧程序

你修改了 `main.py`，再次运行 `python3 tool.py`，输出却和修改前相同。请列出至少三种可能原因，并给出一个按成本从低到高的排查顺序。

<details><summary>最小提示</summary>
先确认运行的文件、当前目录和解释器，再确认程序是否真的读取了你改动的输入。
</details>

<details><summary>讲解与验证</summary>

先用 `pwd` 和 `python3 -c 'import os; print(os.getcwd())'` 确认当前目录，再用 `ls -l`、`head` 或程序内的临时标记确认实际执行的文件；接着检查是否存在同名脚本、缓存或生成产物覆盖输入，最后才调查程序逻辑。边界是某些工具会从项目根目录或环境变量寻找配置，当前目录相同也不代表输入相同。常见错误是只重复按运行键，或直接删除所有文件而丢失可定位证据。验证应记录绝对不必写入正文的路径、输入摘要和退出码，并用一个故意改变的输出确认新代码确实生效。游戏映射：Unity/UE 构建、资源导入和脚本工具中的“改了却没生效”，通常首先是路径或派生产物身份问题，而不是玩法逻辑本身。
</details>
