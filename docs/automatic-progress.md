# 自动续学与独立使用

## 网站会保存什么

访问课程页面时，浏览器会在本地记录：

- 最后阅读的课程页面；
- 最近的标题位置；
- 滚动位置。

再次打开网站首页时，可以自动回到上次位置，也可以选择留在首页或清除记录。

## 为什么适合公开分享

记录只保存在当前浏览器的 `localStorage` 中：

- 不上传 GitHub；
- 不写入仓库；
- 不需要账号；
- 不同访问者互不影响。

不同设备、不同浏览器和无痕窗口不会自动同步。清除网站数据后，阅读位置也会消失。
